# ############################## #
#       Generic Aliases
# ############################## #

alias myip="dig +short myip.opendns.com @resolver1.opendns.com"

# ############################## #
#       MacOS Aliases
# ############################## #

if [[ "$OSTYPE" == "darwin"* ]]; then
    # Ruby
    alias rclean="rdd && rdc && rdm && rds"

    # General
    alias svim="source ~/.vimrc"
    alias szsh="source ~/.zshrc"
    alias salias="source ~/.aliases"
    alias evim="$EDITOR ~/.vimrc"
    alias ezsh="$EDITOR ~/.zshrc"
    alias ealias="$EDITOR ~/.aliases"
    alias vim="mvim -v"
    alias v="vim"
    alias y="youtube-dl"
    alias please='sudo $(fc -ln -1)'
    alias getip="curl -s http://wtfismyip.com/text"

    # Directory Navigation
    alias dl="cd ~/Downloads"
    alias dk="cd ~/Desktop"
    alias doc="cd ~/Documents"
    alias mux="tmuxinator"

    # Stuff
    alias dud='du -d 1 -h'
    alias duf='du -sh *'

    # dir stack
    alias dh='dirs -v'

    # Other navigation stuff
    alias ..='cd ..'
    alias ...='cd ../..'

    # Work stuff
    alias cdw="ssh -i ~/.ssh/id_rsa cab@ec2-54-174-202-186.compute-1.amazonaws.com"
fi
